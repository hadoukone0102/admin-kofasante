import { DonationTypeColorPipe } from './donation-type-color.pipe';

describe('DonationTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DonationTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
