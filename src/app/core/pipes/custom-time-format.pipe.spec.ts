import { CustomTimeFormatPipe } from './custom-time-format.pipe';

describe('CustomTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
